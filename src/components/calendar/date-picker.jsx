export function generateDatePicker() {
  const x = Array.from({ length: 6 }, (_, i) => i);
  const y = Array.from({ length: 7 }, (_, i) => i);

  const dp = x.map((num, index) => {
    <div>{child}</div>
    const v = y.map()
  })
  console.log(x, y)
}
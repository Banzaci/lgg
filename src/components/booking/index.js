import React from 'react'
import PropTypes from 'prop-types';
import { Comtainer } from './style';

const Calendar = ({ dates }) => {
  return (
    <Comtainer>
      
    </Comtainer>
  )
}

Calendar.propTypes = {
  date: PropTypes.array.isRequired,
}

export default Calendar
